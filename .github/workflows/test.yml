# .github/workflows/test.yml
# manual trigger: 
name: Test OIDC Auth #no rule. must be readable, shows up in the "Actions" tab

on: # trigger. Other choices: push, pull_request, schedule (cron job), workflow_call (another workfow calls this)
  workflow_dispatch:  # manual trigger only

permissions:
  id-token: write   # allows GitHub to mint the JWT
  contents: read #allows workflow to read repo code

jobs:
  test-auth:
    runs-on: ubuntu-latest
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Verify access
        run: |
          az account show
          az group show --name rg-resume



          